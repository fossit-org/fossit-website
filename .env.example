# Example .env for fossit_website_dev (Vite app)
# Use `VITE_` prefix for variables that must be exposed to the browser.
# Copy to `.env` (or `.env.production`) and set real values in your deployment environment.

# Public site settings
VITE_SITE_URL=https://fossit.co.in

# API endpoint used by the website (if any)
VITE_API_URL=https://fossit.co.in/api

# Optional tracking / monitoring keys
VITE_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
VITE_SENTRY_DSN=
VITE_RECAPTCHA_KEY=

# Optional: environment name
VITE_ENV=production
